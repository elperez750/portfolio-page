import React from 'react'

function Button({text}) {
  return (
  
      <button type="submit" className='bg-red px-10 py-4 rounded-sm font-futura_demi text-xl'>{text}</button>
  
  )
}

export default Button
